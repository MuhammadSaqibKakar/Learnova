# Chatbox Service (Future)

Reserved folder for the future Learnova chatbox backend.

Keep this service separate from `core-api` so chat can scale and deploy independently.

Typical future responsibilities:

- realtime messaging (WebSocket / Socket.IO)
- chat moderation hooks
- message history APIs
- presence / typing indicators
